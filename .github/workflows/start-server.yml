name: Start Server with ngrok
on: [workflow_dispatch]  # Manually trigger

jobs:
  start-server:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install ngrok
        run: |
          curl -sSL https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
          echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt update && sudo apt install ngrok

      - name: Start ngrok and server
        run: |
          ngrok config add-authtoken ${{ secrets.NGROK_AUTH_TOKEN }}
          ngrok http 3000 --log=".ngrok.log" &
          sleep 5
          URL=$(grep -o -E "https?://[^ ]+" .ngrok.log)
          echo "Public URL: $URL"
          npm install
          nohup node server.cjs &  # Start your Express.js server
          echo "Server running on $URL"
